/* eslint-disable react/prop-types */

const Title = ({ title }) => {
  return (
    <div className="text-[32px] font-[700] font-avenir text-[#333] uppercase">
      {title}
    </div>
  )
}

export default Title